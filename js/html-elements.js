const countriesContainer = document.getElementById('countriesContainer');
const teplateCard = document.getElementById('teplateCard');
const search = document.getElementById('search');
const region = document.getElementById('region');
const searchNull = document.getElementById('searchNull');
const infoButton = document.getElementById('infoButton');
const detailFlag = document.getElementById('detailFlag');
const detailName = document.getElementById('detailName');
const detailNative = document.getElementById('detailNative');
const detailPopulation = document.getElementById('detailPopulation');
const detailRegion = document.getElementById('detailRegion');
const detailSubRegion = document.getElementById('detailSubRegion');
const detailCapital = document.getElementById('detailCapital');
const detailDomain = document.getElementById('detailDomain');
const detailCurrency = document.getElementById('detailCurrency');
const detailLanguage = document.getElementById('detailLanguage');
const detailsContainer = document.getElementById('detailsContainer');
const borderCountriesEl = document.getElementById('borderCountries');
const skeletonContainer = document.getElementById('skeletonContainer');

export {
  countriesContainer,
  teplateCard,
  search,
  region,
  infoButton,
  searchNull,
  detailCapital,
  detailCurrency,
  detailDomain,
  detailFlag,
  detailLanguage,
  detailName,
  detailNative,
  detailPopulation,
  detailRegion,
  detailSubRegion,
  detailsContainer,
  borderCountriesEl,
  skeletonContainer,
};
